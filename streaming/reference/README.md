# Streaming Reference Calls

Illustrative inputs and visual references used before the empirical capture work:

- `typical-diffusion-call.json` — representative image-generation request
- `video-gen-call.json` — representative video-generation request
- `text-to-audio.json` — representative text-to-audio request
- `diffusion-demo-github.mp4` — visual diffusion reference

These are reference materials, not captured streaming evidence.

